#import torch
import time
#device = torch.device('cuda:1' if torch.cuda.is_available() else 'cpu')
#device = torch.device('cpu')
device = 'f'

print(device)
print('\n\n\n')
asdfasdfasdf
time.sleep(5)
print('bye')
